
All kernel tests are enabled and built by default.

To disable individual kernel tests and remove them from the build
use the '--disable-<test-name>' configure option.

For example, remove the smd kernel test from the build
 $ ./configure --disable-smd


Build Instructions

 $ ./autogen.sh
 $ ./configure
 $ make

